ext {
    compileSdkVersion = 33  // or 34, match your installed SDK
}

// buildscript {
//     ext {
//         compileSdkVersion = 33       // ✅ or 34, based on your installed SDK
//         minSdkVersion = 21
//         targetSdkVersion = 33
//         kotlin_version = '1.9.10'    // or whatever version you're using
//     }

//     repositories {
//         google()
//         mavenCentral()
//     }

//     dependencies {
//         classpath 'com.android.tools.build:gradle:8.1.1' // ✅ adjust if needed
//     }
// }

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
ext {
    compileSdkVersion = 33
}

